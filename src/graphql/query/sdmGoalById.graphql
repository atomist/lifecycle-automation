query sdmGoalById($id: ID!) {
  SdmGoal(id: $id) {
    id
    goalSet
    goalSetId
    environment
    uniqueName
    name
    sha
    branch
    repo {
      name
      owner
      providerId
    }
    fulfillment {
      method
      name
    }
    description
    url
    externalUrl
    state
    externalKey
    ts
    error
    retryFeasible
    preConditions {
      environment
      name
    }
    approval {
      correlationId
      registration
      name
      version
      ts
      userId
      channelId
    }
    approvalRequired
    preApproval {
      correlationId
      registration
      name
      version
      ts
      userId
      channelId
    }
    preApprovalRequired
    provenance {
      correlationId
      registration
      name
      version
      ts
      userId
      channelId
    }
    data
  }
}
